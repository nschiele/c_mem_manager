#include "mem.h"
#include <stdio.h>
#include <stdlib.h>


void* getmem(uintptr_t size){

  //  if (root == NULL) {
    //malloc constant part of memory

    //tricky part is special cases eg front of the list. update root. or dont find one big enough and have to add to end
  // }

  return NULL;
}
